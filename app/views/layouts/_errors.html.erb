<% if flash[:error].present? %>
  <div id="error_explanation">
    <h3><%= pluralize(flash[:error].count, "error") %> prohibited this obj from being saved:</h3>
    <ul>
      <% flash[:error].each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>